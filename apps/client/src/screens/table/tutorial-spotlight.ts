/**
 * Tutorial control ring — presentation only (technical spec v6 §5.4 / L45-05).
 * Orange matches shop selected chrome; the server still filters legality.
 */

export const TUTORIAL_SPOTLIGHT_CLASS =
  'ring-2 ring-cta-orange ring-offset-2 ring-offset-surface';
